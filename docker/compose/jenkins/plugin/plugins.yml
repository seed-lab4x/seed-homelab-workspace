# Get it from the Jenkins 2.440.2 installation page 
plugins:
  # Organization and Administration
  - artifactId: dashboard-view
  - artifactId: cloudbees-folder
  - artifactId: configuration-as-code
  - artifactId: antisamy-markup-formatter
  # Build Features
  - artifactId: build-name-setter
  - artifactId: build-timeout
  - artifactId: config-file-provider
  - artifactId: credentials-binding
  - artifactId: embeddable-build-status
  - artifactId: ssh-agent
  - artifactId: throttle-concurrents
  - artifactId: timestamper
  - artifactId: ws-cleanup
  # Build Tools
  - artifactId: ant
  - artifactId: gradle
  - artifactId: msbuild
  - artifactId: nodejs
  # Build Analysis and Reporting
  - artifactId: cobertura
  - artifactId: htmlpublisher
  - artifactId: junit
  - artifactId: warnings-ng
  - artifactId: xunit
  # Pipelines and Continuous Delivery
  - artifactId: workflow-aggregator
  - artifactId: github-branch-source
  - artifactId: pipeline-github-lib
  - artifactId: pipeline-stage-view
  - artifactId: conditional-buildstep
  - artifactId: parameterized-trigger
  - artifactId: copyartifact
  # Source Code Management
  - artifactId: bitbucket
  - artifactId: clearcase
  - artifactId: cvs
  - artifactId: git
  - artifactId: git-parameter
  - artifactId: github
  - artifactId: gitlab-plugin
  - artifactId: p4
  - artifactId: repo
  - artifactId: subversion
  # Distributed Builds
  - artifactId: matrix-project
  - artifactId: ssh-slaves
  # User Management and Security 
  - artifactId: matrix-auth
  - artifactId: pam-auth
  - artifactId: ldap
  - artifactId: role-strategy
  - artifactId: active-directory
  # Notifications and Publishing
  - artifactId: email-ext
  - artifactId: emailext-template
  - artifactId: mailer
  # Appearance
  - artifactId: dark-theme
  # Languages
  - artifactId: locale
